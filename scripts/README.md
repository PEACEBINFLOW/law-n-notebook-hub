# Scripts

Small helper scripts for this repo.

- `build_notebook_index.py` — optional helper that scans `notebooks/core/` and updates `docs/notebook-index.md`.

You can extend this folder with:

- batch converters
- dataset preparation scripts
- Kaggle export helpers
